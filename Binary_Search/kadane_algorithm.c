#include <stdio.h>
#include <assert.h>
#include <inttypes.h>
#include <limits.h>

#define N (int)1e9+7
void solve();

void solve(){
int n,i,j;
assert(fscanf(stdin,"%d",&n)>0);
int a[N],maxi=0;
memset(a,0,sizeof(a));
a[0]=1;
for(i=0;i<=n-1;i++)
{
assert(fscanf(stdin,"%lld",&x)>0);
a[x]+=1;
maxi=(maxi<x)?(x):(maxi);
}
for(i=2;i<=n-1;i++)
{
 for(j=1;j<=n-1;i++)
 {
     dp[]		
 }	 
}
}


int main()
{
	int tt=1;
	while((tt--)>0){
		solve();
	}
}
